# 更新日志

## 待提交 · 2026-09-15

记录当前工作区相对 `78e8f08` 的改动，发布状态未核实。

### 功能更新

- 赠品选择新增“仅允许一个赠品规格”和“仅选择有库存的赠品”开关，仅在本次编辑中生效。启用后使用规格选择器，确认选择时重新核对库存；单规格限制不改变赠送件数。
- 规格选择器支持单个 `handle:` 条件搜索，先查找对应产品，再定位其规格；未找到产品时显示错误。
- 活动列表分别展示主商品、赠品和参与规格数。主商品按排除规则统计，赠品按规格去重；主商品规格未读全时显示“规格待确认”，长名称可通过提示查看全文。
- 页面展示全部已保存活动的配置用量，以 UTF-8 字节统计，容量提示与保存校验共用 10000 字节上限。当前草稿不计入用量，未导入的旧配置和新店显示“尚未保存”。

### 修复

- 保存成功后，等待旧折扣停用处理结束再释放同店保存锁，防止下一次保存与旧折扣处理交错。

### 开发维护

- 页面入口改为 `app-home.tsx`，将共用组件、业务工具函数和类型整理到 `components`、`utils`、`types`，文件名统一使用小写烤串命名。
- 测试迁移到 `src/__test__/*.test.ts`，使用 Vitest，并新增 `test`、`test:watch` 命令。类型检查覆盖源码、测试和 Vitest 配置。

## 已提交 · 2026-09-09 · `78e8f08`

- 合并活动编辑页的状态管理，统一异步操作入口和防重复操作逻辑；加载失败时保留已有页面数据与草稿。
- 活动状态按开始、结束时间自动刷新，无需手动刷新页面。
- 重试保存前重新核对已准备的 Shopify 折扣，保存完成后清理对应缓存。
