{
  "name": "nuphy-extension-only-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "node scripts/run-shopify-environment.mjs development app build --config shopify.app.toml",
    "dev": "node scripts/run-shopify-environment.mjs development app dev --config shopify.app.toml",
    "info": "shopify app info --config shopify.app.toml",
    "generate": "shopify app generate",
    "deploy": "node scripts/run-shopify-environment.mjs development app deploy --config shopify.app.toml",
    "dev:test": "node scripts/run-shopify-environment.mjs development app dev --config shopify.app.toml",
    "info:test": "shopify app info --config shopify.app.toml",
    "dev:61style": "node scripts/run-shopify-environment.mjs development app dev --config oooyi",
    "info:production": "shopify app info --config production",
    "info:japan": "shopify app info --config japan",
    "deploy:test": "node scripts/run-shopify-environment.mjs development app deploy --config shopify.app.toml",
    "deploy:production": "node scripts/run-shopify-environment.mjs production app deploy --config production",
    "deploy:japan": "node scripts/run-shopify-environment.mjs production app deploy --config japan",
    "validate:test": "shopify app config validate --config shopify.app.toml --json",
    "validate:production": "shopify app config validate --config production --json",
    "validate:japan": "shopify app config validate --config japan --json",
    "build:test": "node scripts/run-shopify-environment.mjs development app build --config shopify.app.toml",
    "build:production": "node scripts/run-shopify-environment.mjs production app build --config production",
    "build:japan": "node scripts/run-shopify-environment.mjs production app build --config japan"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "Administrator",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "devDependencies": {
    "@graphql-codegen/cli": "5.0.5"
  }
}
